0.1 概述
在本节中，我们提出 TPRL，一个用于大型视觉语言模型（LVLMs）中视觉 token
剪枝的强化学习框架。我们方法的整体架构如图 1所示。首先，我们在第0.2节中将 token
剪枝任务形式化为马尔可夫决策过程（MDP），定义了状态空间、动作空间和多目标奖
励函数。我们先对策略网络进行预训练0.3，然后，我们在第0.4节详述了基于强化学习的
1
自适应剪枝机制，包括策略网络和价值网络架构。接着，我们在第0.5节介绍了完整的训
练策略和推理协议，使用近端策略优化（PPO）和组相对策略优化（GRPO）两种不同
的策略优化算法进行实验比较。这个综合性框架实现了动态视觉 token 剪枝，在 LVLMs
中最佳地平衡了计算效率和任务性能。
0.2 语言引导的 MDP 建模
为了实现 LVLMs 中自适应且高效的 token 剪枝，我们将问题形式化为马尔可夫决
策过程（MDP）。这种建模方式允许模型通过顺序决策来学习剪枝策略，同时利用视觉
和文本信息，而无需依赖手工设计的启发式规则或复杂的相关性评分函数。MDP 由元
组 (S, A,P, R, γ) 定义，其中 S、A、P、R 分别代表状态空间、动作空间、状态转移动
态和奖励函数，γ ∈ [0, 1] 代表折扣因子。
状态空间。为了准确表示所有对决策有用的信息，我们将当前未处理的视觉 token 和文
本查询都纳入状态空间。通过将这些多模态信号输入策略网络，模型能够学习评估视觉
token 与文本 token 之间的相关性以及单个视觉 token 的重要性，从而做出数据驱动的
剪枝决策。第 t 步的状态 st 正式定义为：
st = (Ht
, q) (1)
其中 Ht ∈ R
Kt×d 代表当前未处理的视觉 token 的特征（Kt 是第 t 步未处理 token 的数
量），q ∈ R
dq 是文本查询嵌入。关键的是，在每一轮剪枝之后，所有已被判断为被剪枝
的 token 都会被永久地从考虑范围中移除，这确保了马尔可夫性质。这种状态表示随着
剪枝的进行而动态演化，Kt 随时间减少，为做出明智的剪枝决策提供了必要的上下文。
动作空间。为了保持学习的可处理性，同时实现对 token 级别的精确控制，我们设计了
一个动作空间，使其在每一轮中同时评估所有未处理的 token。在第 t 轮，智能体为 Ht
中的每个 token 输出一个二元决策：
at = {di
| i ∈ {0, 1, . . . , Kt − 1}, di ∈ {0, 1}} (2)
其中 di = 0 表示剪枝 token i，di = 1 表示保留它。状态转移定义为：
Ht+1 = {ht,i | di = 1} (3)
其中 ht,i 表示第 t 步 Ht 中的第 i 个 token。这种建模确保了所有已被判断为被剪枝的
token 在每一轮后都被永久移除，而保留的 token 进入下一轮继续考虑，动作空间随着
剪枝的进行而自然地适应。
奖励函数。为了引导学习过程在任务性能和计算效率之间达到最佳平衡，我们设计了一
个增量式奖励函数，用于衡量每轮剪枝的边际影响。至关重要的是，为了减少奖励方差，
我们计算的是小批量样本而非单个实例的奖励。每一步的奖励由两个精心设计的组件组
合而成：
rt = Rtask + Reff(t) (4)
2
其中 rt 表示第 t 步的总奖励，Rtask 表示任务性能奖励分量，Reff(t) 表示第 t 步的效率
奖励分量。
任务奖励通过加权批次平均任务准确率的边际变化，鼓励保持下游性能：
Rtask = α ·
1
B
∑
B
b=1
[
TaskScore(H
(b)
t+1, q(b)
) − TaskScore(H
(b)
t
, q(b)
)
]
(5)
其中 α 是任务性能的权重系数，B 是批次大小，上标 (b) 表示批次中的第 b 个样本。这
种批次平均显著降低了奖励方差，同时保留了我们奖励设计的增量性质。
效率奖励通过奖励批次内的平均剪枝比例来促进计算量减少：
Reff(t) = β ·
1
B
∑
B
b=1 (
1 −
K
(b)
t+1
K
(b)
t
)
(6)
其中 β 是效率的权重系数，K
(b)
t 是样本 b 在第 t 步的未处理 token 数量，K
(b)
t+1 是第
t + 1 步的相应数量。
0.3 预训练策略网络。
如图 2预训练的代码和数据集标注我来做，需要确保代码中的 policy network 可以
进行预训练，预留相应的框架。策略网络面临着探索效率低下的挑战，从头开始训练时
可能会收敛到次优策略。我们使用高质量的剪枝示范进行监督学习预训练。这些示范是
使用现有的启发式剪枝方法（例如，基于注意力机制的评分、基于梯度的重要性）离线
生成的，这些方法提供了合理的剪枝轨迹，尽管并非最优。
示范生成。对于训练集中的每个图像-文本对，我们运行一个启发式剪枝器来生成一系
列剪枝决策。演示轨迹 τ 被记录为状态-动作对序列 (st
, at)，其中状态 st 包括压缩的视
觉表示 Zt 和文本查询 q，而 at 是启发式方法在步骤 t 推荐的二元剪枝动作。
0.4 基于 RL 的自适应 token 剪枝
基于 MDP 建模，我们设计了一个强化学习框架，通过学习策略实现自适应 token
剪枝。其核心创新在于一个参数高效的架构，其中策略网络和价值网络共享一个基于注
意力的特征提取器，能够在最小化计算开销的同时对 token 关系进行联合建模。在训练
阶段，我们将所有输入图像调整为固定分辨率（例如 224×224），这样经过视觉编码器
后，每个图像都会产生固定数量的视觉 token（例如 196 个）。这确保了在训练时每个样
本的视觉 token 数量相同，从而可以批量处理，同时避免图片过大影响训练效率。
共享注意力模块。为了高效捕捉视觉 token 之间及其与文本查询的相关性，我们设计了
一个共享注意力模块，作为策略网络和价值网络的共同特征提取器。该模块的输入包括
视觉 token 特征和文本查询嵌入：
X = [Ht
; ˜q] ∈ R
(N+1)×d
(7)
3
其中 Ht ∈ R
N×d 代表视觉 token 特征，q˜ ∈ R
d 是通过线性投影对齐后的查询嵌入，X
表示拼接后的序列。
共享注意力模块通过带残差连接的多头自注意力处理此输入：
Attention(X) = softmax (
XWQ(XWK)
⊤
√
dk
)
XWV (8)
Z = LayerNorm(X + Concat(head1, . . . , headh)WO) (9)
其中 WQ, WK, WV ∈ R
d×dk 和 WO ∈ R
hdk×d 是可学习参数，dk = d/h 是每个头的维度，
h 是注意力头数。这种注意力机制对所有 token 与查询之间的成对关系进行建模，为策
略和价值估计提供了丰富的上下文特征。
策略网络。策略网络接收注意力输出 Z = [z1, z2, . . . , zN , zN+1]，其中 zi 对应视觉 token，
zN+1 对应文本 token。对于每个视觉 token i，我们通过一个带残差连接的轻量级 MLP
计算其保留概率：
fi = GELU(W1zi + b1) (10)
gi = LayerNorm(zi + W2fi + b2) (11)
pt,i = σ(w
⊤gi + b) (12)
其中 W1 ∈ R
d×d
, W2 ∈ R
d×d
, w ∈ R
d 是可学习参数，b1, b2, b 是偏置项，σ 是 sigmoid 激
活函数。gi 中的残差连接防止了训练期间的梯度消失。最终动作 at = {(i, di)} 通过采样
di ∼ Bernoulli(pt,i) 决定，其中 di = 1 表示保留 token i，di = 0 表示剪枝它。
价值网络。价值网络共享相同的注意力模块，但使用不同的头部进行状态价值估计。它
通过全局平均池化聚合所有未处理 token 的注意力输出，然后经过一个带残差连接的
MLP：
z¯ =
1
Kt
∑
Kt
i=1
zi (13)
v = GELU(U1z¯ + c1) (14)
Vϕ(st) = u
⊤LayerNorm(¯z + U2v + c2) + bv (15)
其中 U1 ∈ R
d×d
, U2 ∈ R
d×d
, u ∈ R
d 是可学习参数，c1, c2, bv 是偏置项。残差连接确保了
优化过程中稳定的梯度流。
0.5 训练与推理
0.5.1 实验设置：PPO 与 GRPO 对比
在本研究中，我们进行两组实验，分别使用两种不同的策略优化算法：
1. PPO（近端策略优化）：作为基准算法，采用标准的 PPO 框架进行训练。
2. GRPO（组相对策略优化）：采用组相对策略优化算法，该算法通过组内相对比较
来估计优势函数，可能在某些场景下提供更稳定的训练动态。
4
两组实验共享相同的网络架构和奖励设计，仅在策略优化算法上有所不同，以便公
平比较两种方法在视觉 token 剪枝任务上的性能。
PPO 训练优化。对于 PPO 实验组，我们采用近端策略优化（PPO）来训练策略网络
和价值网络。我们训练方法的一个关键方面是带有随机掩码的稀疏剪枝协议，它显著提
高了样本效率，同时保留了空间关系。
在训练期间，我们将剪枝过程限制在 Tmax 轮，并使用高阈值 τtrain = 0.7，以保持保
守的剪枝行为。在第 1 轮剪枝前，我们对 20% 的未处理 token 应用随机掩码（代码层
面设置一下超参，在第一轮剪枝前是否采取随机掩码，对百分之几的 token 进行掩码）
以加快剪枝训练速度：
H
masked
t = Ht ⊙ mt
, mt ∼ Bernoulli(0.8) (16)
其中 mt 是二元掩码向量。这个掩码操作在创造多样观察条件的同时，保持了 token 的
空间顺序。
对于每个输入样本（图像-文本对），我们执行 Tmax = 5 轮剪枝，每轮产生一个独立
的训练样本。形式化地，给定 M 个输入样本，我们每个周期生成 M ×5 个训练样本。为
了计算 PPO 中使用的优势 Aˆ
t，我们采用广义优势估计（GAE），它通过参数 λ ∈ [0, 1]
权衡偏差和方差。首先定义一步时序差分（TD）残差：
δt = rt + γVϕold(st+1) − Vϕold(st), (17)
其中 γ 是折扣因子，Vϕold 是当前更新前参数的价值网络。GAE 优势估计量计算为未来
TD 残差的指数加权和：
Aˆ
t =
T
∑−t−1
l=0
(γλ)
l
δt+l (有限时域实现), (18)
在实践中，为数值稳定性通常通过反向递归实现。可选地，为了整个批次的稳定性，我
们对优势进行归一化：
Aˆ
t ←
Aˆ
t − mean(Aˆ)
std(Aˆ) + ϵ
, (19)
其中 ϵ 是一个小常数（例如，10−8）。
价值函数更新的目标值则为：
V
target
t = Aˆ
t + Vϕold(st), (20)
这对应于与 GAE 优势一致的经验回报估计量。
PPO 使用的裁剪策略损失为：
L
CLIP(θ) = Et
[
min (
rt(θ)Aˆ
t
, clip(
rt(θ), 1 − ϵ, 1 + ϵ
)
Aˆ
t
)] , (21)
其中 rt(θ) = πθ(at|st)
πθold(at|st) 是概率比，ϵ 是 PPO 裁剪超参数。
5
价值损失最小化与目标的平方误差：
L
VF(ϕ) = Et
[(
Vϕ(st) − V
target
t
)2
]
. (22)
熵正则化项鼓励探索：
L
S
(θ) = Et

−
∑
i:ut,i=1
(pt,i log pt,i + (1 − pt,i)log(1 − pt,i))

 . (23)
组合这些部分得到总优化目标：
LPPO(θ, ϕ) = −L
CLIP(θ) + c1L
VF(ϕ) − c2L
S
(θ), (24)
其中 c1 和 c2 是平衡价值回归和熵的系数。
GRPO 训练优化。对于 GRPO 实验组，我们采用组相对策略优化算法。GRPO 通过
组内样本的相对比较来估计优势函数，而不是依赖价值函数估计。具体来说，我们为每
个批次中的样本分组，计算组内相对优势：
AˆGRPO
t =
rt − µgroup
σgroup + ϵ
, (25)
其中 µgroup 和 σgroup 分别是当前组内奖励的均值和标准差。GRPO 的优化目标为：
LGRPO(θ) = Et
[
min (
rt(θ)AˆGRPO
t
, clip(
rt(θ), 1 − ϵ, 1 + ϵ
)
AˆGRPO
t
)] . (26)
GRPO 不需要单独的价值网络，简化了训练架构。
推理协议。在推理期间，我们采用与多步训练过程显著不同的一次性剪枝策略。给定输
入图像和文本查询，策略网络同时处理所有视觉 token，并为每个 token 输出保留概率
{pi}
N
i=1，其中 N 是视觉 token 的总数。然后，我们应用预定义的阈值 τ 来做出二元剪
枝决策：如果 pi > τ 则保留 token i，如果 pi ≤ τ 则将其剪枝。
这种单次剪枝方法确保了部署时的计算效率，因为它只需要策略网络的一次前向传
播。阈值 τ 可以调整，以实现计算效率和任务性能之间的不同权衡，从而灵活适应各种
资源限制。请注意，在推理过程中，τ 可调以适应效率-精度权衡，而 τtrain 在训练期间
是固定的，以保证策略学习的稳定性。剪枝后的 token 集 {xi
| pi > τ} 随后被馈送到下
游的 LVLM 进行最终任务执行，在显著减少计算开销（与处理所有视觉 token 相比）的
同时保持模型性能。
